import ContactContent from "@/components/site/contact/contact-content";

export const metadata = {
  title: "Contact Us",
  description: "Reach the federation office for anything from maintenance to membership.",
};

export default function ContactPage() {
  return <ContactContent />;
}
